# .env.example — Kopiere diese Datei zu ".env" und fülle die Werte aus
# WICHTIG: Füge ".env" zu .gitignore hinzu! (API-Keys niemals committen)

# OpenAI API-Key (optional — Pipeline läuft auch ohne)
# Kostenlos testen: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-api-key-here

# Optional: GPT-Modell überschreiben
# OPENAI_MODEL=gpt-3.5-turbo
# OPENAI_MODEL=gpt-4
